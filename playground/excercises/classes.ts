// Create a class called Racer which takes a name and a gender parameter in its constructor.
// We expect gender to be given as 'male' or 'female'.

// Create a class called Guest which takes a name and a budget parameter in its constructor.
// The budget means how much money the guest has to spend on bets.
// If a name is not given, you should store the value 'Anonymous'

// Create a class called Race which stores a list of racers and a list of guests.

// It should have a method called 'girlPower' which returns a list of Racers whose gender is 'female'.

// It should have a method called 'allTheMoney' which returns the sum of the budgets of all the Guests.

// It should have a raceResults method which returns the names of the Racers in a random order.